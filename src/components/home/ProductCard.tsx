import { ArrowRight, PackageSearch } from "lucide-react";
import { Link } from "react-router-dom";
import type { Product } from "../../data/catalog";
import { resolveCutoutImage, resolveProductImage } from "../../data/productImageResolver";
const brl = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });
export function ProductCard({ product }: { product: Product }) { const image = resolveCutoutImage(product) || resolveProductImage(product); const previous = product.previousPrice && product.previousPrice > product.minPrice ? product.previousPrice : product.avgPrice > product.minPrice ? product.avgPrice : undefined; return <article className="pc-product"><Link to={`/produto/${product.slug || product.id}`} className="pc-product__image" aria-label={`Ver ${product.name}`}>{image ? <img src={image} alt={product.name} loading="lazy" /> : <PackageSearch aria-hidden="true" />}</Link><div className="pc-product__body"><span className="pc-store-badge">{product.establishment || "Mercado parceiro"}</span><h3>{product.name}</h3><span className="pc-product__meta">{product.size || product.category || "Preço atualizado"}</span><div className="pc-prices">{previous && <del>{brl.format(previous)}</del>}<strong>{brl.format(product.minPrice)}</strong></div><Link className="pc-compare" to={`/produto/${product.slug || product.id}`}>Comparar preços <ArrowRight /></Link></div></article>; }
